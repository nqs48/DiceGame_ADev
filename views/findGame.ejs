<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>🎲 DiceGame | Find Game </title>
    <link href="../public/styles.css" rel="stylesheet" />
  </head>
  <body class="bg-gradient-to-b from-cyan-400 to-cyan-50 h-screen">
    <%- include('components/header'); %>
    <div class="h-4/5">

    
    <div class="flex flex-row justify-center items-center">
      <form id="formFindGame" class="m-10 flex">
        <label for="gameID" class="sr-only">Game ID</label>
        <input
          id="id"
          name="idName"
          type="text"
          required
          class="w-80 rounded-l-lg p-4 border-t mr-0 border-b border-l text-gray-800 border-gray-300 focus:outline-none focus:ring-gray-50 focus:border-gray-50 bg-white"
          placeholder="Game ID"
        />
        <button
          type="submit"
          class="px-8 rounded-r-lg bg-cyan-400 active:bg-cyan-500  text-white font-bold p-4  border-t border-b border-r focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-white"
        >
          Search
        </button>
      </form>
    </div>
    <div class="flex justify-center mt-24">
        <img
          class="block lg:hidden h-40 w-40"
          src="https://res.cloudinary.com/adev48/image/upload/v1652573714/Sofka%20Assets/Semana%204/dices_tzeuva.png"
          alt="Workflow"
        />
        <img
          class="hidden lg:block h-40 w-40"
          src="https://res.cloudinary.com/adev48/image/upload/v1652573714/Sofka%20Assets/Semana%204/dices_tzeuva.png"
          alt="Workflow"
        />
      </div>
      </div>
      <%- include('components/footer'); %>
    <script>
      const formFindGame = document.querySelector("#formFindGame");
      formFindGame.addEventListener("submit",async (e) => {
        e.preventDefault();
        const ide= formFindGame.elements['idName'].value;
        const id= document.querySelector("#id").value;
        id.toString();
        console.log("id Game: ", id);
       try {
        const data = await fetch(`/findGame/${ide}`, {method: 'GET'});
            window.location.href = `/findGame/${id}`;
        } catch (e) {
          console.log(e);
        }
      });
    </script>
  </body>
</html>
